-Implement a handler for CTRL+C during scanning which will terminate the scan and save the results acquired so far.

-Differentiate between different objects with same pathname/basename.

-Add option to export results to .csv instead of .pscan.
